schema_version = 1
harness = "pi"
model = "openai-codex/gpt-6-astra"
args = ["--thinking", "xhigh"]
protocol = true

[sections]
mission = "Investigate and produce an implementation-ready plan with evidence, options, recommendation, dependencies, and acceptance checks. Keep the task read-only unless implementation is explicitly authorized."
