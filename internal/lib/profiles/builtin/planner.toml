schema_version = 1
harness = "codex"
model = "gpt-6-astra"
args = ["-c", "model_reasoning_effort=xhigh"]
role = "Investigate and produce an implementation-ready plan with evidence, options, recommendation, dependencies, and acceptance checks. Keep the task read-only unless implementation is explicitly authorized."
