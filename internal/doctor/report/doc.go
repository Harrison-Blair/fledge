// Package report defines the doctor report model and its rendering.
// report.go defines statuses, checks, the summary tally, and ReportError;
// data.go defines each check's structured data; render.go writes the grouped
// human report or the JSON document.
package report
