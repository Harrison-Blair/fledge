---
description: Plan a fledge feature — interrogate and author plumages and feathers
argument-hint: "<feature request>"
---
Load and follow `.fledge/skills/fledge-orchestrate/SKILL.md`, then route to the **planning** phase (read `planning.md` in that skill's directory) and execute it for the following request:

$@

Before driving the phase, read `.pi/fledge-adapter.md` to see how fledge's primitives map to pi in this harness. (This adapter provides the four Tier A primitives; foraging runs in-session per `planning.md` step 2.)
