---
description: Implement fledge feathers — run the implementation phase
argument-hint: "[PLM-### | FTHR-### | ready]"
---
Load and follow `.fledge/skills/fledge-orchestrate/SKILL.md`, then route to the **implementation** phase (read `implementation.md` in that skill's directory) and execute it for:

$@

Before driving the phase, read `.pi/fledge-adapter.md` to see how fledge's primitives map to pi in this harness. (This adapter provides the four Tier A primitives, so implementation runs solo per `implementation.md` §2 — you implement each ready feather yourself, gating with the user. No team loop.)
