# EXP3 task file — one representative, meaty prompt per line (real, sustained work).
# Blank lines and lines starting with # are ignored. Need >= n non-comment lines.
#
# IMPORTANT (methodology): run panes in a NEUTRAL cwd (--cwd ~/exp3-scratch), and
# keep these prompts free of throttle vocabulary and of this repo's contents.
# Otherwise the pane-output throttle detector matches the work itself, not a real
# rate limit (that invalidated the first n=2 run, which pointed panes at fledge).
# These tasks are self-contained: Claude generates from scratch, reads no repo.
Implement an LRU cache in Go with O(1) get and put, backed by a map plus a doubly linked list; write table-driven unit tests covering eviction order and updates, and run them until green.
Write a backtracking Sudoku solver in Python with a clean API, add pytest cases for a solvable board, an already-solved board, and an unsolvable board, and run them.
Explain TCP congestion control (slow start, congestion avoidance, fast retransmit, fast recovery) in a Markdown doc with a worked packet-by-packet example and an ASCII cwnd-over-time chart.
Design a URL shortener: data model, base62 id scheme, collision handling, and read/write paths; write the core encode/decode in Go with unit tests and run them.
Implement Dijkstra's shortest path over an adjacency-list graph in Python with a binary-heap priority queue, add tests on a small weighted graph, and run them.
