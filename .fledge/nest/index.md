---
generated: 2026-07-10T20:53:53Z
commit: f28efebd76d6aa135adb0956a3337a40a8d98351
agent: fledge-forager
fledge_version: 0.3.0
---

# Context Index

## architecture.md
Explains the two-layer split (deterministic `internal/cli`+domain packages vs. the `internal/bootstrap` embed/adapter/manifest scaffolding system), the 6-primitive/tier-derivation model, manifest file write policies, and the scaffold-stamp drift/preserve/prune mechanics. Also covers cross-module wiring (how init.go, nest.go, preen.go compose the domain packages) and this repo's own dogfooding loop (`pluma/`, `.claude/` generated by the very code they exercise).
Read this when: you need to understand how the CLI and bootstrap layers relate, how adapters/tiers/primitives work, or how scaffold drift/refresh is computed — especially before touching `internal/bootstrap` or planning a new adapter subagent.

## modules.md
Repo map organized by top-level module (`<root>`, `cmd`, `docs`, `pluma`, and `internal` split into `bootstrap`/`cli`/core-packages) with purpose, key files, and "Look here for" pointers per module.
Read this when: you need to orient in an unfamiliar part of the repo or decide which module(s) a change touches before diving into code.

## conventions.md
Reconciled coding/process conventions: deterministic spec mutation rules, acceptance-criteria checkbox mechanics, bird terminology, Go/CLI idioms (command registration, error helpers, flag parsing, atomic writes), bootstrap file-write-policy semantics, and the txtar fixture update rule.
Read this when: you're about to write or modify code and want to match existing style, or need to know which test fixtures must be updated alongside a bootstrap/adapter change.

## data-model.md
Every core Go struct/type across `internal/spec`, `check`, `graph`, `lock`, `nest`, `repo`, `scan`, and `internal/bootstrap` (Manifest, Stamp, Drift, etc.), plus each CLI command's JSON output shape and the status-transition matrices.
Read this when: you need to know a type's exact fields, JSON shape, or where a struct is defined before writing code that touches it.

## dependencies.md
The full external dependency list (`go-yaml`, `go-internal`/testscript, two indirect x/ packages), notable stdlib usage (embed, sha256, os/exec for git, text/template), and a note that `docs/google_ai_mode_response.md`'s referenced services are NOT real fledge dependencies.
Read this when: you're evaluating whether to add a new dependency, or need to know what's already available for YAML/templating/git-shelling/hashing.

## entry-points.md
Build/test/run commands, the CLI dispatcher and every registered command's flags/behavior, the bootstrap package's public functions (`LoadAdapters`, `WriteCore`, `WriteAdapter`, `DriftReport`), and the agent-facing orchestration entry points (SKILL.md, foraging.md, fledge-adapter.md).
Read this when: you need the exact command to run something (build, test, a specific `fledge` subcommand) or want the full list of a command's flags/behavior before invoking or modifying it.

## testing.md
Both test styles in detail: the 20 testscript/txtar acceptance fixtures under `cmd/fledge/testdata/` (with per-fixture coverage summary and the "must-update" rule for bootstrap/adapter changes) and the unit test suite per package (with specific test function names).
Read this when: you're adding a feature and need to know which fixture(s) to extend, or writing/debugging a test and want to see existing coverage patterns to match.

## domain.md
Glossary of fledge's bird-themed vocabulary — spec lifecycle terms (plumage, feather, pipping, fledged, AC/FC), workflow-role terms (forager, scout, brood, brooder, skua, orchestrator, preen, molt, vee, colony, wave, ready), and bootstrap/adapter terms (harness, primitive, tier, adapter, manifest, scaffold, drift, concern doc).
Read this when: you encounter an unfamiliar bird-themed term anywhere in this repo (code, specs, or other nest docs) and need its precise definition.

## Open Questions

Unresolved items carried forward from synthesis live at the bottom of the relevant concern doc (architecture.md, conventions.md, data-model.md, testing.md). None are blocking; all are minor implementation-detail curiosities noted by scouts.
</content>
